{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm install -g gmgn-cli && npx skills add GMGNAI/gmgn-skills"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
